const average = 10.5;
const status = average >= 10 ? "Aprobado" : "Reprobado";
console.log(status);

let max = 0;

const a = 10;
const b = 20;
const c = 30;

max = a > b ? a : b;
max = max > c ? max : c;
console.log(max);
